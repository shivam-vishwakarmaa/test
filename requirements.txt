# Core
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
PyYAML>=6.0

# LLM
anthropic>=0.40

# Only needed if you don't have Ollama and want the fully-offline TF-IDF
# embedding fallback -- already covered by scikit-learn above.
